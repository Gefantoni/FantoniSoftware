{
  "version": 2,
  "cleanUrls": true,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/landing/api/$1" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/landing/api/$1" },
    { "src": "/(.*)", "dest": "/landing/$1" }
  ]
}
